@model PostViewModel

@{
    ViewData["Title"] = "Post Details";
}

<div class="twoColorDiv bg-danger">
    <h2 class="text-center">@Model.Title</h2>
</div>
<div class="container">
    @*<div class="row">
        <div class="col-12">
            <div class="titleRow row d-flex">
                <div class="col-12">
                    <p class="fw-bold fs-4 text-center">@Model.Title</p>
                </div>
            </div>
        </div>
    </div>*@
    <div class="row">
        <div class="col-lg-7">
            <div class="contentRow row mt-3 shadow-sm" style="width: auto; background-color: #F5F7FD;">
                <div class="col-12">
                    <p class="lead text-dark">@Model.Content</p>
                </div>
            </div>
        </div>
        <div class="col-lg-5">
            <div class="imagesRow row mt-3">
                @foreach (var img in Model.ImageUrls)
                {
                    <div class="col-12 mt-2 mb-4">
                        <div class="ratio ratio-4x3">
                            <img class="rounded img-fluid" src="@img" alt="Club Image">
                        </div>
                    </div>
                    <hr class="border border-danger border-2 opacity-50">
                }
            </div>
        </div>
    </div>
</div>